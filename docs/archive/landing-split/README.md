# Landing Split Archive

Este directorio conserva documentación histórica del split Landing/Platform.

- `docs/migrate/*`: plan y runbooks usados durante el proceso de separación.
- `docs/migate/*`: alias legacy mantenido por compatibilidad histórica.
- `docs/debug/*`: logs y errores específicos de la app Landing durante la transición.

Estado actual del repositorio:

- El código activo es solo PLATFORM.
- Landing quedó fuera del árbol operativo del proyecto.

